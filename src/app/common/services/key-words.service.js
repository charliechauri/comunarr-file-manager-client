export class KeyWordsService {
    constructor($http, $q, CacheFactory) {
        this.$http = $http;
        this.$q = $q;
        this.CacheFactory = CacheFactory;
    }

    get() {
        
    }
}